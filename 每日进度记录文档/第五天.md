# 第五天

## 代办

- [x] 完成回收站逻辑

## 设计

- 删除笔记或者删除文件夹就更新`is_deleted`字段为`1`，并设置`parentId`为`-1`
- 回收站显示逻辑：
  - 查找数据库中，`parentId`为`-1`并且`is_deleted`字段为`1`的
  - 和 Windows 回收站一样，不允许查看被删除的文件夹内部结构
  - 恢复时恢复到笔记管理的 root 目录即可